import React from 'react'

export type TMockJson = {
    name:string; 

}

export const JSON_DATA_MOCK = [
    {
        name: "",
    },
]

export const SelectedCardOption = ({ data }: { data: void | undefined }) => {
    return (
        <>
        </>
    )
}
